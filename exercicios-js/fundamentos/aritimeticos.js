/* Operadores aritimeticos são binario, pos operam sempre
com dois operandos */

const [a, b, c, d] = [3, 5, 1, 15]

const soma = a + b + c + d
const subtraçcao = d - b
const multiplicacao = a * b
const divisao = d / a
const modulo = a % 2

console.log(soma, subtraçcao, multiplicacao, divisao, modulo)
